name: My First Workflow

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    run: |
      echo "Server date:"
      date
      echo "Current user:"
       whoami
  
    steps:
      - name: Print message
        run: echo "Hello, GitHub Actions!"
